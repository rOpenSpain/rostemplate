# ros_metro_madrid_pal() limita solicitudes excesivas

    Code
      colors <- ros_metro_madrid_pal(14, alpha = 1)
    Condition
      Warning:
      `ros_metro_madrid_pal()` solo dispone de 12 colores, menos de los solicitados (14).

