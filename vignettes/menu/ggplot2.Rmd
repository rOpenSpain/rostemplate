---
title: "ggplot2 example"
description: "Tests rendering of a ggplot2 figure inside the articles menu."
---

```{r}
#| include: false
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>",
  dev = "ragg_png"
)
```

Example using a **ggplot2** image.

```{r}
#| label: setup
#| fig-alt: A ggplot2 image
#| fig-cap: A ggplot2 image
library(ggplot2)

# Counts or sums of weights.
g <- ggplot(mpg, aes(class))
# Number of cars in each class.
g + geom_bar()
```
